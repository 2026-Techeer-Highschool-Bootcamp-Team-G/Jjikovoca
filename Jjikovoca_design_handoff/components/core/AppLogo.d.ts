/**
 * 앱 아이콘 마크(정사각 라운드 스퀘어). 하단 촬영 FAB, 로그인/온보딩 히어로에 사용.
 */
export interface AppLogoProps {
  /** 정사각 한 변. FAB 56, 로그인 96 */
  size?: number
}
export declare function AppLogo(props: AppLogoProps): JSX.Element
