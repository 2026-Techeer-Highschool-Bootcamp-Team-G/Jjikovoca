단어 카드/행의 태그 묶음 — 시험 태그를 앞으로 당기고 일반 태그는 2개까지만 보여준다.

```jsx
<TagList tags={[{ label: '중간고사', kind: 'exam', dday: 7 }, { label: '#수능특강' }, { label: '#중요' }, { label: '#동사' }]} />
```

- 정렬 규칙은 컴포넌트가 강제한다(시험 태그 → 일반 태그 → +N). 호출하는 쪽에서 직접 정렬하지 말 것.
- 카드 목록은 `max={2}`, 단어 상세는 `max={99}` 로 전부 펼친다.
