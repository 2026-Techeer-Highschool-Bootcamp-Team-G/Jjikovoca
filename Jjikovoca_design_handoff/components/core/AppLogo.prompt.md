브랜드 마크 — 뷰파인더 브래킷(찍다)과 노란 형광펜 스트립(보카)의 조합.

```jsx
<AppLogo size={56} />
```

- 그림자를 줄 때는 래퍼에 `borderRadius: var(--radius-logo-13)`(56px 기준)을 맞춰 형태를 따라가게 한다.
- 워드마크는 별도 파일이 없다. 로고타입이 필요하면 "찍어보카"를 20px/700 `--color-text-brand` 로 적는다.
- 파일 형태는 `assets/logo.svg`.
