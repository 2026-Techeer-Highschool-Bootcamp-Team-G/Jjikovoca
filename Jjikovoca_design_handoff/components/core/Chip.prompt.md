가로 스크롤 분류 필터 — 단어장의 전체/졸업완료/복습대기/약점유형처럼 한 줄로 늘어놓는다.

```jsx
<Chip active={status === 'ALL'} onClick={() => setStatus('ALL')}>전체 24</Chip>
```

- 개수를 아는 분류만 숫자를 붙이고, 모르면 라벨만 표시한다.
- 선택 상태는 한 그룹에 하나. 다중 선택 UI는 이 시스템에 없다.
