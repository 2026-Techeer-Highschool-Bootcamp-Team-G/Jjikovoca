복습 판정 버튼 — 플래시카드·빈칸·수학 복습이 모두 같은 3버튼을 쓴다.

```jsx
<GradeButtons onGrade={(g) => next(g)} />
```

- 순서 고정: 몰라요(danger-weak) → 헷갈려요(brand-weak) → 알아요(brand fill).
- 높이 48px, 균등 분할. 라벨을 다른 표현으로 바꾸지 않는다.
