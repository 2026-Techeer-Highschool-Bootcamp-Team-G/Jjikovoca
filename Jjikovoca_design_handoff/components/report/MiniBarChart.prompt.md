리포트의 주간 학습 시간 분포 막대 차트 (FR-09).

```jsx
<MiniBarChart data={[{ label: '월', value: 12 }, { label: '화', value: 24 }, { label: '수', value: 8 }]} />
```

- 7칸(요일)까지가 적정. 축·격자선은 그리지 않는다.
- 최댓값만 진한 파랑, 나머지는 `--color-brand-weak`.
