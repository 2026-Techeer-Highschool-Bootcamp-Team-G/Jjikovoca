연속 학습일을 칠하는 잔디 캘린더 (FR-14, GitHub 잔디 레퍼런스).

```jsx
<StreakGrid streakDays={16} days={window.REPORT.grass} />
```

- 강도는 0~4 다섯 단계(`--color-grass-0`~`4`)로만 표현한다 — 초록이 아니라 브랜드 파랑 계열이다.
- 리포트 탭과 마이 탭 성장 카드에서 함께 쓴다.
